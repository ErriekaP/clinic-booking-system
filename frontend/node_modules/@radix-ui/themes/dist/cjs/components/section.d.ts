import * as React from 'react';
import { sectionPropDefs } from './section.props';
import type { MarginProps, LayoutProps, GetPropDefTypes } from '../helpers';
type SectionOwnProps = GetPropDefTypes<typeof sectionPropDefs>;
interface SectionProps extends React.ComponentPropsWithoutRef<'div'>, MarginProps, LayoutProps, SectionOwnProps {
}
declare const Section: React.ForwardRefExoticComponent<SectionProps & React.RefAttributes<HTMLDivElement>>;
export { Section };
export type { SectionProps };
