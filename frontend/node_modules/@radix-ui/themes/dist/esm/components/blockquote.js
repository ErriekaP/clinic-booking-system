import * as React from 'react';
import classNames from 'classnames';
import { Text } from './text';
const Blockquote = React.forwardRef((props, forwardedRef) => {
    const { children, className, ...blockquoteProps } = props;
    return (React.createElement(Text, { asChild: true, ...blockquoteProps, ref: forwardedRef, className: classNames('rt-Blockquote', className) },
        React.createElement("blockquote", null, children)));
});
Blockquote.displayName = 'Blockquote';
export { Blockquote };
//# sourceMappingURL=blockquote.js.map