'use client';
import * as React from 'react';
import classNames from 'classnames';
import * as CheckboxPrimitive from '@radix-ui/react-checkbox';
import { checkboxPropDefs } from './checkbox.props';
import { extractMarginProps, withMarginProps, withBreakpoints } from '../helpers';
import { ThickCheckIcon } from '../icons';
const Checkbox = React.forwardRef((props, forwardedRef) => {
    const { rest: marginRest, ...marginProps } = extractMarginProps(props);
    const { className, style, size = checkboxPropDefs.size.default, variant = checkboxPropDefs.variant.default, color = checkboxPropDefs.color.default, highContrast = checkboxPropDefs.highContrast.default, ...checkboxProps } = marginRest;
    return (React.createElement("span", { className: classNames('rt-CheckboxRoot', className, withBreakpoints(size, 'rt-r-size'), withMarginProps(marginProps)), style: style },
        React.createElement(CheckboxPrimitive.Root, { "data-accent-color": color, ...checkboxProps, ref: forwardedRef, className: classNames('rt-reset', 'rt-CheckboxButton', `rt-variant-${variant}`, {
                'rt-high-contrast': highContrast,
            }) },
            React.createElement(CheckboxPrimitive.Indicator, { className: "rt-CheckboxIndicator" },
                React.createElement(ThickCheckIcon, { className: "rt-CheckboxIndicatorIcon" })))));
});
Checkbox.displayName = 'Checkbox';
export { Checkbox };
//# sourceMappingURL=checkbox.js.map